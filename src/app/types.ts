export interface UserProps {
    id: string
    createdAt: Date
    phone: string
    name: string
    dogName: string
    breedAndColor: string
    email: string
    wantsPrints: string
    edited: boolean
    sent: boolean
    match: boolean
}